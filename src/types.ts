export interface TUser {
    id: string,
    name: string,
    email: string,
    password: string,
    createdAt: string
}

export interface TUserDB {
    id: string,
    name: string,
    email: string,
    password: string,
    created_at: string
}
